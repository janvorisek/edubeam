export enum MouseMode {
  NONE,
  SELECTING,
  HOVER,
  MOVING,
  ADD_NODE,
  ADD_ELEMENT,
  ADD_DIMLINE,
  PASTE_CLIPBOARD,
  /** Dragging a rectangle over the drawing to choose what an image export shows. */
  PICK_WINDOW,
}
